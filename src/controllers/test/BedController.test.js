import BedController from '../BedController';

describe.skip('BedController', () => {
  it('should return error if value is not a string');
  it('should return error if value is not a valid string');
  it('should return success if only valid string');
});